- flash.each do |key, value|
  - key = 'info' if %[alert notice].include?(key)
  .alert{ class: "alert-#{key}" }
    %button{ type: "button", class: "close", data: {dismiss: "alert"} } &times;
    = value
